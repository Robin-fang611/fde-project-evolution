# 贡献指南

欢迎贡献三类内容：

1. **业务场景**：补充真实但已脱敏的中小企业流程。
2. **样例数据**：提供虚构但贴近真实的表格、聊天记录、系统导出。
3. **工具脚本**：补充零依赖或低依赖的可运行 demo。

## 场景贡献格式

```markdown
## 场景名称

### 企业/部门类型

### 当前流程

### 当前痛点

### 当前数据来源

### 建议试点

### 评价指标

### 数据和权限风险
```

## 样例数据要求

- 必须是虚构数据或充分脱敏数据。
- 不得包含真实客户、员工、供应商、合同、发票、手机号、地址、账号、密钥。
- 要保留真实业务的混乱感，例如：客户名称不完全一致、日期格式不统一、金额有小误差。
- 文件里要明确标注“模拟数据，仅供学习”。

## 代码要求

- 优先使用 Python 标准库。
- 不新增依赖，除非先说明原因和替代方案。
- 输出要可被人工复核，不要直接替用户做高风险决策。
- 错误信息要让非工程用户也能理解。

## PR 建议

- 一个 PR 只做一类改动。
- 新增 demo 时，至少包含 `README.md`、`sample_data/`、`run.py`。
- 新增方法论文档时，尽量提供已填写示例。
